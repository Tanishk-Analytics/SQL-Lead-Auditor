# SQL-Lead-Auditor

git init
git add lead_auditor_v1.sql
git commit -m "Initial commit: SQL Schema for Lead Performance Auditing"
git branch -M main
git remote add origin [YOUR_NEW_REPO_URL]
git push -u origin main
